# Attendance Service — config base (sin secretos)
spring.application.name=attendanceService
server.port=8093

spring.datasource.url=jdbc:postgresql://localhost:5432/librodigital_attendance
spring.datasource.username=postgres

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.open-in-view=false

academic.service.url=http://localhost:8092

spring.profiles.active=${SPRING_PROFILES_ACTIVE:local}
